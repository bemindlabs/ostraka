# Template profile. Copy it, point `command` at a CLI on your PATH, and run
# `ostraka adapters list` to confirm the runtime can find it.
id = "example"
command = "example-cli"
args = ["--print", "{{prompt}}"]
event_format = "none"

[capabilities]
headless = true
streams_json = false
resumable = false
